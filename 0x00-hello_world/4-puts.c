#include <stdio.h>
/**
 *main - All codes are
 *  to be written in
 *   the main function
 *   Return: always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
