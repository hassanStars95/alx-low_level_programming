#include "main.h"


/**
 * main - tests function that print if an integer is positive or negative
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
